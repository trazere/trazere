<?xml version="1.0" encoding="UTF-8"?>
<!--
  Copyright 2006-2013 Julien Dufour

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.
-->
<project>
	<import file="env.xml" />
	<import file="${ant.project}" />
	<import file="${parser.project}" />
	<import file="${utils.project}" />

	<!-- Declarations -->
	<property name="root.resources" location="${root}/resources" />

	<property name="root.dist" location="${root}/dist" />
	<property name="root.dist.binary" location="${root.dist}/trazere-${trazere.version}" />
	<property name="root.dist.binary.libs" value="${root.dist.binary}/libs" />
	<property name="root.dist.binary.maven" value="${root.dist.binary}/maven" />
	<property name="root.dist.binary.sources" value="${root.dist.binary}/sources" />
	<property name="root.dist.tgz" location="${root.dist}/trazere-${trazere.version}.tar.gz" />
	<property name="root.dist.zip" location="${root.dist}/trazere-${trazere.version}.zip" />

	<!-- Clean -->
	<target name="root.clean" depends="ant.clean, parser.clean, utils.clean" />

	<!-- Clean the distribution -->
	<target name="root.distclean">
		<delete dir="${root.dist}" />
	</target>

	<!-- Compile -->
	<target name="root.compile" depends="ant.compile, parser.compile, utils.compile" />

	<!-- Publish the artifacts -->
	<target name="root.publish" depends="ant.publish, parser.publish, utils.publish" />

	<!-- Build the distribution -->
	<target name="root.build" depends="ant.buildJar, ant.buildPom, ant.buildSource, parser.buildJar, parser.buildPom, parser.buildSource, utils.buildJar, utils.buildPom, utils.buildSource">
		<mkdir dir="${root.dist.binary}" />

		<!-- Libs -->
		<mkdir dir="${root.dist.binary.libs}" />
		<copy todir="${root.dist.binary.libs}">
			<fileset file="${ant.target.artifacts.jar}" />
			<fileset file="${parser.target.artifacts.jar}" />
			<fileset file="${utils.target.artifacts.jar}" />
			<mapper type="glob" from="*-jar.jar" to="*.jar" />
		</copy>

		<!-- Sources -->
		<mkdir dir="${root.dist.binary.sources}" />
		<copy todir="${root.dist.binary.sources}">
			<fileset file="${ant.target.artifacts.source}" />
			<fileset file="${parser.target.artifacts.source}" />
			<fileset file="${utils.target.artifacts.source}" />
			<mapper type="glob" from="*-source.jar" to="*-sources.jar" />
		</copy>

		<!-- Maven -->
		<mkdir dir="${root.dist.binary.maven}" />
		<copy todir="${root.dist.binary.maven}">
			<fileset file="${ant.target.artifacts.pom}" />
			<fileset file="${parser.target.artifacts.pom}" />
			<fileset file="${utils.target.artifacts.pom}" />
			<mapper type="glob" from="*-pom.pom" to="*.pom" />
		</copy>

		<!-- Distribution -->
		<copy todir="${root.dist.binary}">
			<fileset dir="${root.resources}">
				<include name="README.txt" />
				<include name="LICENSE.txt" />
			</fileset>
		</copy>
	</target>

	<!-- Build the archives -->
	<target name="root.buildArchives" depends="root.build">
		<tar destfile="${root.dist.tgz}" basedir="${root.dist}" compression="gzip">
			<include name="trazere-${trazere.version}/**/*" />
		</tar>
		<zip destfile="${root.dist.zip}" basedir="${root.dist}" encoding="iso-8859-1">
			<include name="trazere-${trazere.version}/**/*" />
		</zip>
	</target>
</project>
