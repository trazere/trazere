<?xml version="1.0" encoding="UTF-8"?>
<project name="utils" default="usage">
	<!-- Imports -->
	<import file="../../module/modules.xml" />
	<import file="${utils.ant.impl}" />

	<!-- Usage -->
	<target name="usage">
		<echo>TODO</echo>
	</target>

	<!-- Clean -->
	<target name="clean" description="Clean" depends="utils.clean" />

	<!-- Build -->
	<target name="build" description="Build" depends="utils.buildBinary" />
</project>
