<?xml version="1.0" encoding="UTF-8"?>
<!--
  Copyright 2006-2010 Julien Dufour

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.
-->
<project>
	<import file="env.xml" />
	<import file="${tools.project}" />

	<!-- Declarations -->
	<property name="utils.sources" location="${utils}/sources" />

	<property name="utils.target" location="${utils}/build/target" />
	<property name="utils.target.classes" location="${utils.target}/classes" />
	<property name="utils.target.jar" location="${utils.target}/trazere-utils.jar" />

	<property name="utils.dist" location="${utils}/dist" />
	<property name="utils.dist.lib" value="${utils.dist}/lib" />

	<union id="utils.libs">
		<union id="utils.deps">
			<resources refid="tools.ant.libs" />
			<resources refid="tools.commons-logging.libs" />
			<resources refid="tools.sjsxp.libs" />
		</union>
		<union id="utils.jars">
			<fileset file="${utils.target.jar}" />
		</union>
	</union>
	<path id="utils.classpath">
		<pathelement location="${utils.target.classes}" />
	</path>

	<!-- Clean -->
	<target name="utils.clean">
		<delete dir="${utils.target}" />
	</target>

	<!-- Clean the distribution -->
	<target name="utils.distclean">
		<delete dir="${utils.dist}" />
	</target>

	<!-- Compile the sources -->
	<target name="utils.compileSources">
		<mkdir dir="${utils.target.classes}" />
		<javac destdir="${utils.target.classes}" source="${trazere.java.version}" encoding="${trazere.java.encoding}" target="${trazere.java.version}" debug="${trazere.java.debug}" includeantruntime="false" failonerror="true">
			<src location="${utils.sources}" />
			<classpath>
				<path refid="tools.ant.classpath" />
				<path refid="tools.commons-logging.classpath" />
				<path refid="tools.sjsxp.classpath" />
			</classpath>
		</javac>
	</target>

	<!-- Compile -->
	<target name="utils.compile" depends="utils.compileSources" />

	<!-- Build the JAR -->
	<target name="utils.buildJar" depends="utils.compileSources">
		<mkdir dir="${utils.target}" />
		<jar destfile="${utils.target.jar}">
			<fileset dir="${utils.target.classes}" includes="**/*.class" />
			<fileset dir="${utils.sources}" excludes="**/*.java" />
		</jar>
	</target>

	<!-- Build the distribution -->
	<target name="utils.build" depends="utils.buildJar">
		<mkdir dir="${utils.dist}" />

		<!-- Jars -->
		<mkdir dir="${utils.dist.lib}" />
		<copy todir="${utils.dist.lib}">
			<resources refid="utils.jars" />
		</copy>
	</target>

	<!-- Build the bootstrap JAR -->
	<!--
	<target name="utils.buildBootstrapJar" depends="utils.compileSources">
		<echo>Building the bootstrap JAR</echo>

		<mkdir dir="${utils.dist}" />
		<jar destfile="${utils.dist.bootstrap_jar}">
			<fileset dir="${utils.sources}" includes="com/trazere/util/TargetTask.java" />
			<fileset dir="${utils.target.classes}" includes="com/trazere/util/TargetTask.class" />
		</jar>
	</target>
	-->
</project>
