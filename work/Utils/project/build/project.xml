<?xml version="1.0" encoding="UTF-8"?>
<!--
  Copyright 2006-2013 Julien Dufour

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.
-->
<project xmlns:ivy="antlib:org.apache.ivy.ant">
	<import file="env.xml" />

	<!-- Declarations -->
	<property name="utils.sources" location="${utils}/sources" />

	<property name="utils.build" location="${utils}/build" />
	<property name="utils.build.ivy" location="${utils.build}/ivy.xml" />

	<property name="utils.target" location="${utils.build}/target" />
	<property name="utils.target.classes" location="${utils.target}/classes" />
	<property name="utils.target.jar" location="${utils.target}/trazere-utils.jar" />
	<property name="utils.target.source_jar" location="${utils.target}/trazere-utils-sources.jar" />

	<property name="utils.dist" location="${utils}/dist" />
	<property name="utils.dist.lib" value="${utils.dist}/lib" />
	<property name="utils.dist.maven" value="${utils.dist}/maven" />
	<property name="utils.dist.sources" value="${utils.dist}/sources" />

	<path id="utils.classpath">
		<pathelement location="${utils.target.classes}" />
	</path>
	<union id="utils.jars">
		<fileset file="${utils.target.jar}" />
	</union>

	<!-- Clean -->
	<target name="utils.clean">
		<delete dir="${utils.target}" />
	</target>

	<!-- Clean the distribution -->
	<target name="utils.distclean">
		<delete dir="${utils.dist}" />
	</target>

	<!-- Setup the dependencies -->
	<target name="utils.setupDependencies">
		<ivy:resolve file="${utils.build.ivy}" log="download-only" />
		<ivy:cachepath pathid="utils.dependencies.classpath" conf="compile" />
	</target>

	<!-- Compile the sources -->
	<target name="utils.compileSources" depends="utils.setupDependencies">
		<mkdir dir="${utils.target.classes}" />
		<javac destdir="${utils.target.classes}" source="${trazere.java.version}" encoding="${trazere.java.encoding}" target="${trazere.java.version}" debug="${trazere.java.debug}" includeantruntime="false" failonerror="true">
			<src location="${utils.sources}" />
			<classpath>
				<path refid="utils.dependencies.classpath" />
			</classpath>
		</javac>
	</target>

	<!-- Compile -->
	<target name="utils.compile" depends="utils.compileSources" />

	<!-- Build the JAR -->
	<target name="utils.buildJar" depends="utils.compileSources">
		<mkdir dir="${utils.target}" />
		<jar destfile="${utils.target.jar}">
			<fileset dir="${utils.target.classes}" includes="**/*.class" />
			<fileset dir="${utils.sources}" excludes="**/*.java" />
		</jar>
	</target>

	<!-- Build the source JAR -->
	<target name="utils.buildSourceJar">
		<mkdir dir="${utils.target}" />
		<jar destfile="${utils.target.source_jar}">
			<fileset dir="${utils.sources}" />
		</jar>
	</target>

	<!-- Build the distribution -->
	<target name="utils.build" depends="utils.buildJar, utils.buildSourceJar">
		<mkdir dir="${utils.dist}" />

		<!-- Libs -->
		<mkdir dir="${utils.dist.lib}" />
		<copy todir="${utils.dist.lib}">
			<resources refid="utils.jars" />
		</copy>

		<!-- Sources -->
		<mkdir dir="${utils.dist.sources}" />
		<copy todir="${utils.dist.sources}">
			<fileset file="${utils.target.source_jar}" />
		</copy>

		<!-- Maven -->
		<mkdir dir="${utils.dist.maven}" />
		<property name="ivy.pom.version" value="${trazere.version}" />
		<ivy:makepom ivyfile="${utils.build.ivy}" pomfile="${utils.dist.maven}/trazere-utils.pom" conf="compile,runtime" />
	</target>
</project>
